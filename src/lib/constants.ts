/** How early (in minutes) before a lecture's start time a student may check in. */
export const EARLY_CHECKIN_MINUTES = 120;

/** Default geofence radius (meters) applied to a new lecture hall. */
export const DEFAULT_GEOFENCE_RADIUS_METERS = 80;

/** Default minutes after a lecture's end time during which checkout is still accepted. */
export const DEFAULT_CHECKOUT_GRACE_MINUTES = 15;

/** Students below this attendance rate (%) are worth flagging for a teacher. */
export const AT_RISK_THRESHOLD = 75;
